{
  "name": "@chirp/monorepo",
  "private": "true",
  "version": "0.1.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n \"@chirp/core,@chirp/web\" -c \"blue,green\" \"cd packages/web && bun run dev\" \"cd packages/core && bun run dev\"",
    "redis": "docker run --name redis -d --network host redis redis-server --save 60 1 --loglevel info"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
